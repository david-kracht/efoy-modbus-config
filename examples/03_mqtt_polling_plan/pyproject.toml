[project]
name = "example-03-mqtt-polling-plan"
version = "0.1.0"
description = "Build a Modbus→MQTT polling plan from the EFOY schema"
requires-python = ">=3.11"
dependencies = [
    "efoy-modbus-config",
]

[tool.uv.sources]
efoy-modbus-config = { path = "../..", editable = true }
