# The device specified within the package
PKG_NAME=modbus-config

# URL to download the PDF from (best-effort; if download fails, PDF_LOCAL_PATH is used)
PDF_URL=26_02_Modbus_TCP.pdf

# Local path to the PDF file (used if URL download fails or is not set)
PDF_LOCAL_PATH=26_02_Modbus_TCP.pdf

# Output path for the generated JSON specification
#OUTPUT_PATH=packages/modbus-config/src/modbus_config/schemas/v30.json

# Device metadata
DEVICE_NAME="EFOY Fuel Cell"
VERSION=26.02
